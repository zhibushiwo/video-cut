# 开发状态交接（HANDOFF）

> **职责**：会话上下文压缩后的**状态快照**——现在在哪、下一步做什么、未决问题。
> **唯一真源**：**"当前状态与下一步"**以本文为准；进度细节以 [PLAN.md](./PLAN.md) 为准（里程碑表仅在状态变化时同步）；长期红线以 [../AGENTS.md](../AGENTS.md) 为准。
> **读时机**：会话开场第一条；接续他人工作时。
> **写规则**：只写当前状态与**近期相关**的坑；已定型的长期约定移入 AGENTS.md（本文只留指针）；历史批次要点进 [handoff-archive.md](./archive/handoff-archive.md)，本文不堆积。
> **关联**：[../README.md](../README.md)（项目简介/技术栈/命令） · [INDEX.md](./INDEX.md)（地图与 ID） · [PLAN.md](./PLAN.md)（进度真源） · [../AGENTS.md](../AGENTS.md)（红线与完事标准）
> **最后更新**：2026-09-25
> **项目速览**（原独立小节，2026-09-19 去重为指针）：简介 / 功能 / 技术栈 / 环境要求 / 构建命令 → [../README.md](../README.md)｜命令 / 红线 / 完事标准 → [../AGENTS.md](../AGENTS.md)｜文档分工与 ID 规范 → [INDEX.md](./INDEX.md) §1–§2。

## 当前状态（2026-09-25）

> **状态真源是 [PLAN.md](./PLAN.md)**（里程碑总览的状态列）；本表是**快照**，只在状态变化时同步；M0–M9 各批次的提交号与实施要点见 [handoff-archive.md](./archive/handoff-archive.md)。**代码侧现状核对**：`useTauriEvent` / `utils/crop.ts` / `CropOverlay` 已就位；任务系统带终态清理钩子；`pnpm lint` 可用（type-aware 基线，存量 0 problems，耗时 ≈10s）；`src/utils/undo/` 已就位（M11-0）、Vitest 已引入（`pnpm test`，基线 36 条）；R2 五项全部完成（`R2-1` Workbench 拆分 / `R2-2` 重复收敛 / `R2-3` useHotkeys 激活门控 / `R2-4` noUncheckedIndexedAccess + ESLint type-aware / `R2-5` 死代码清理——`App.css`、`TaskStatus::Probing`、5 处 `#[allow(dead_code)]` 均已删）——即 **R2 ✅ 完成、M11 实施全部完成（`M11-0`–`M11-9` ✅；`M11-9` 帧预算真机半 ⏳ 待用户发起）**，下一步 **R3 → M12-2 → M12-1/3 → M13**。另：2026-09-25 完成全仓过度工程审查（ponytail-audit 口径，只看过度工程与复杂度）——真发现 ≈-345 行源码＋≈-60~80 投机测试、0 依赖可砍，立项 `T-005`/`T-006`；节流 ×9 复核归 `R3-4`（原记 7 份）；全文见 [archive/code-review-2026-09-25-overengineering.md](./archive/code-review-2026-09-25-overengineering.md)（**含豁免清单：撤销读回侧 / speed 链 / `submit_with_cleanup` 属已排期或红线固化，勿重复立案**）。

| 里程碑 | 状态 |
| --- | --- |
| M0–M9 基建 → 工作台 → 修复冲刺 | ✅ 已交付（M4-5 实机冒烟、M6-7/M7/M9 待用户统一手测） |
| M10 保活 + 深浅主题 | ⏸ 暂缓（决策 #32） |
| R1 评审修复五项 | ✅ 2026-09-19（`23c3623` `eea9422` `70a991e` + R1-4/R1-5） |
| R2 重构 · R3 收尾 | ✅ R2 全部完成（`R2-1` 2026-09-24；`R2-2`/`R2-3`/`R2-4`/`R2-5` 2026-09-25）；R3 在 M12-2 前——已完成：**`R3-7`（2026-09-24）· `R3-1`/`R3-2`/`R3-3`（2026-09-26）** |
| **R4 第二轮审查整改**（7 条，修 `BUG-001`~`006`；另 `R4-8` 修真机首跑缺陷 `BUG-007`~`009`、`R4-9` 修同族 `BUG-010`） | ✅ **9 条全部实施完毕**（2026-09-21 ~ 09-23）；缺陷状态：`BUG-001`/`002`/`007`/`009` = `verified`，`003`~`006`/`008`/`010`/`011` = `fixed`（差的只是各自手工/真机那一半） |
| M11 单轨时间线核心 | 🔄 **实施全部完成**：`M11-0` 状态层/撤销基座 ✅（2026-09-24，含 Vitest 载体与 `TC-040`）、`M11-1` 精度基座 ✅、`M11-2` 缩放滚动 ✅、`M11-3` 播放头路径 ✅、`M11-4` 选择与波纹删除 ✅、`M11-5` 切割 ✅、`M11-6` 边缘修剪 ✅、`M11-7` 撤销接线 ✅、`M11-8` 菜单与快捷键 ✅、`M11-9` 性能验收 ✅（2026-09-25；帧预算真机半 ⏳ = TC-043 手工半，待用户发起） |
| M12 预览强化 → M13 打磨 | ⏳ M11 之后 |

## 下一步（按序）

**R3 → M12-2 → M12-1/3 → M13**（R2 五项全部完成、M11 实施全部完成——`M11-9` 帧预算真机半 ⏳ 待用户发起，2026-09-25）；M10 与时间线核心零耦合，视反馈随时插入。**R4 段 9 条已全部实施完毕**（2026-09-21 ~ 09-23）。

- **已做的最近十二批**：
  - （2026-09-26）**`R3-2` + `R3-3`** 任务系统收尾（R3 第二批，R3-2 去重统一管理 / R3-3 internal 不可见）——新增 `TaskManager::submit_internal(sink, kind, label, dedup_key, job)`：同 `(kind, dedup_key)` 活动任务**复用既有 taskId**（不重复入队、不 emit_status），登记表 `(kind,key)→taskId` 由 TaskManager 持有、`run_cleanup` **终态统一回收**（完成/失败/取消含排队取消全路径），提交入口重构为私有 submit_core（旧 `submit`/`submit_with_cleanup` 逐语句等价，机制按红线保留）；generate_proxy 三步手工簿记（自建 HashMap + cleanup 钩子 + is_active 自愈）整体迁移、删无调用方的 `TaskManager::is_active`，**顺带消除旧"submit 后再登记"两步竞态**；snapshot() 过滤 internal（任务面板补元数据 + 关闭守卫不可见），`StatusPayload` 加 `internal` 标记、**事件照常派发**（useProxyPreview 完成事件链路不受影响），TaskProgress 凭标记跳过建行、progress 对未知行天然 no-op；TaskSnapshot **不加**字段（CR 抓到死字段——先过滤再构造恒 false）；历史落盘 kind 白名单不改即正确。规格同步：DESIGN §5.4/§7/§8.2、UI §9.7、AGENTS 第 8 条（去重登记不自建）。**CR 双轴 PASS**（规格 4P2/规范 6P2 全不阻塞；采纳 3 条）。新增 4 条测试（隐藏+事件照发 / 活动期复用 / 终态回收 / 排队取消回收）。验证：cargo 87+5 · tsc/eslint 0 · vitest 92 · check-docs 五绿。**已提交**（代码 `4a8c6d4` + 本次 `docs:` 回填）。
  - （2026-09-26）**`R3-1`** probe 缓存改 LRU 逐条淘汰（R3 收尾首批，工程批次，无行为变化——缓存命中路径多一次戳刷新）——四张探测缓存（media/facts/keyframe/duration）从"≥512 整体 `clear`"改为 **LRU 逐条淘汰**：`HashMap<ProbeKey, LruEntry<T>>{value, stamp}` + 全局原子戳计数器（`next_stamp`，锁内分配，LRU 序无偏差），`cache_get` 命中刷新戳、`cache_put` 达限且为新键时 `min_by_key(stamp)` 逐出最久未用一条再插入、**覆盖已有键只刷新不逐出**；动机：M12-2 自动渲染预览高频提交探测，整体清空会周期性清光关键帧缓存导致反复重扫（R3 排在 M12-2 前的原因）。规格先行：FFMPEG.md §6.5 缓存量级防御条改 LRU 口径（含覆盖不逐出半句）、删"⚠ 待改"行；AGENTS.md §8 坑表残留"≥512 整体清空"同步（CR 规范轴 P1）。cap 测试重写为 LRU 语义（len 不变 / 被触碰条目存活 / 逐出的确是最久未用者）+ 按规格轴 P2 建议补"达限覆盖不逐出"边界测试（独立缓存防并行互踩、先取值后断言防锁中毒连锁）。**CR 双轴 PASS**（规格 2×P2 建议全采纳；锁中毒路径与旧实现同口径不 panic、probe.rs clippy 零警告）。验证：cargo 83 单测 + 5 e2e · tsc/eslint/vitest 92 · check-docs 五绿。**已提交**（代码 `bad3096` + 本次 `docs:` 回填）。
  - （2026-09-25）**`M11-9`** 性能验收（M11 收官批；**经用户裁决不做真机自动化取数**）——playing 场景埋点接线（ProductPreview rAF effect 起播开窗/停播冲刷，§18.6 三场景全就位）；**撤销预算计量**（纯函数层临时脚本，数字记 TC-043 自动化半）：100 片段重排 execute p50=0.003ms/max=0.289ms、undo max=0.126ms、redo max=0.011ms——**比 16ms 预算低两个数量级，命令层不是瓶颈**（composite×100 max=1.955ms、split/trim ≤1.3ms 一个数量级余量；不落永久单测防 flaky）；**React.memo 裁决：不立项**（复核条件 = 真机半 drag P95 超预算）；**三项权衡收口**（手势内 Ctrl+Z/菜单快捷键活跃维持披露接受、全局右键维持不做）；**帧预算真机半固化 [gui-e2e/cases-m11-perf.md](./gui-e2e/cases-m11-perf.md)**（TC-043 手工半 ⏳，100 夹具生成命令 + 判定线 P95 ≤ 16.7ms / seek < 100ms 已写入）。**CR**：合并双轴（Spec PASS / Standards 一轮 FAIL 1P1+5P2 → 全修后复审确认六项成立；TC-043 熔行第三次复发——python 表格锚点教训已入 MEMORY §2）。**已提交**（代码 `116b46a` + 本次 `docs:` 回填）。
  - （2026-09-25）**`M11-8`** 菜单与快捷键（行为批次，§17.4 菜单表 + `FR-1738`/`FR-1739` 发号）——自研 `components/ContextMenu`（createPortal + fixed + 视口钳制每渲染重测 + Escape/外部按下/再按右键三路关闭）；片段菜单六条目（切割按播放头命中禁用 / 波纹删除 / **移除并删除池片段确认后走命令栈可撤销**——兑现 §17.5 可撤销删除入口 / 加工 / 撤销·重做带操作名与空栈禁用）+ 空白菜单三条目（适应窗口经 `zoomFitRef` 桥接）；空白 pointerdown button 门禁 + 手势中不开菜单（块+视口双侧）。快捷键补全：K/L 走带（`PLAYBACK_RATES` 单一来源；倍率经 ProductPreview 新 prop 落双槽，新媒体加载重置 → metadata 补挂；非 1× 徽标）、A 追加三态、Ctrl+E（`exportDisabled` 提升共用）、I/O 修剪选中片段；C/S 键 e.code 统一（M11-7 注记⑧兑现）。`blockAtTime` 抽 geometry 配 3 用例（M11-5"勿复制"兑现）。**块点击裁决**：选中+进加工保留为长期行为。**双轴 CR**：一轮 FAIL（文档 P1×3——TESTING TC-041/042 熔行[历史事故]、INDEX 漏更 + 代码 P2×10）→ 全修后双轴复审 PASS；披露：e.code 物理键位取舍、菜单打开期间快捷键仍活跃（注记⑨）、全局右键不做 suppress（⑩）、a11y 记后续（⑪）。单测 89→92。**手测点**：块右键六条目、移除并删除确认+可撤销、空白右键+适应窗口、右键不误 seek、K/L 加速徽标、A 追加三态、Ctrl+E、I/O 修剪、Esc 关菜单。**已提交**（代码 `298b433` + 本次 `docs:` 回填）。
  - （2026-09-25）**`M11-7`** 撤销接线（行为接线批次，§17.5/§17.8 + `FR-1736`/`FR-1737` 发号）——Ctrl+Z / Ctrl+Shift+Z 全模式可用（时间轴常驻可见、与 Delete 同口径；栈空自然 no-op；输入焦点忽略内建；`e.code === "KeyZ"` 布局无关判定 + 排除 Alt），M11-8 右键菜单撤销/重做项复用同一对 handler；**§18.9 复活清洗裁决（`FR-1737`）**：携带片段的外部删除毒化全部历史 → 真删了片段才**整体清栈**（`useUndoStack.clear` 接入 `deleteFromPool`/`removeSource`；无片段素材的删除不清栈——files 不在文档快照），`applyRaw` docstring 写明"删 clips 必须配 clear()"；**快照语义披露**：撤销/重做恢复全量文档（含彼时 rot/crop/lockRatio——栈顶命令之后的旁路编辑会被随后的撤销回退，单测锁定）；加工视图死引用兜底 effect；no-op 不清检测缓存（同族六处，修掉"页脚停在正在检测"隐患）。**双轴 CR**：Spec 一轮 FAIL（1P1+5P2）修至复审 PASS；Standards PASS（5P2 全采纳）。**文档事故披露**：M11-6 批次注记编辑误删 plans/M11.md 的 M11-5 注记（随 `fd1481c` 入库），本批逐字恢复（对 `97c2dc6` 基准核实）。单测 87→89。**手测点**：Ctrl+Z/Shift+Z 撤销重做全链、栈空按 Z 页脚不变、池卡 ✕ 后撤销失效、删无片段素材后历史保留、输入框内 Ctrl+Z 走文本撤销、撤销掉编辑中片段时预览区回成品模式。**已提交**（代码 `4446899` + 本次 `docs:` 回填）。
  - （2026-09-25）**`M11-6`** 边缘修剪（行为批次 + `BUG-012` 裁决，plans/M11.md §18.5 落地 + 注记①~⑬）——`geometry.findTrimEdge`（热区全局最近 ≤8px，共享边界按**指针侧归属**消歧）+ `snapToKeyframe`（8px/pps，未加载静默降级）；beginPointerDrag ≥4px 激活、`computeTrim` 预览/提交共用、松手入栈一条、Esc/pointercancel（按 `last.type`）丢弃、激活后吞 click、挂 drag 埋点；**入边拖动左缘被前缀和钉死**（连续模型几何使然，指针位移映射源入点增量），出边界跟手且不钳 contentWidth（末块可延伸到源尾）；tooltip 时间码+时长+徽标实时（ref+300ms 尾随防抖融合 payload）；S 键 write-through（App 新透传 `updateSettings`）+ Alt 旁路 + 关键帧按源懒加载；双击边缘 = 修剪到播放头（`locateProduct` 与切割共用）。**`BUG-012` 裁决**：命令层下限 `minSegSec = max(1帧, 0.05s)`，前端 `exportSegmentOf` 与后端 `commands::valid_segment_span`（cut/pipeline 共用）边界一致（1µs 容差）。**双轴 CR 一轮 FAIL 后修至 Standards 复审 PASS**（Spec 复审代理因基础设施故障未跑完，其 P0/P1 已逐一代码核实）——P0 = 后端 `> start+0.05` 严格校验未随裁决对齐（恰 0.05s 段整单导出硬失败）；教训：**阈值对齐类裁决要把同一谓词的全链消费方 grep 干净**。单测 79→87、cargo 81→82。**手测点**：8px 热区 ew-resize、出边跟手/入边块宽收缩+tooltip、徽标实时、S/Alt 吸附、双击到播放头、Esc 丢弃、修剪松手不切加工视图、末块向右拉伸。**已提交**（代码 `fd1481c` + 本次 `docs:` 回填）。
  - （2026-09-25）**`M11-5`** 切割（首个行为接线批次的 builder 直交样本，plans/M11.md §18.5 落地 + M11-5 注记）——`splitAtPlayhead`：playheadRef 权威值 → timeline 前缀和命中（与 `buildSplit` 内部 offset 求和**同式同源**、浮点逐位一致）→ 成品时间直交 builder（源内换算/≥1 帧校验/no-op 全在 builder，M11-0 既有）；C 键 useHotkeys（product 模式且有片段，**修饰键守卫**防 Ctrl+C 误触）；页面零新增校验、undo 层零改动、缩略图按新 key 自动重取；右键菜单入口归 M11-8（届时命中检测抽 geometry 勿复制）。**两轴 CR PASS**；采纳 P1 = **`BUG-012` 登记（open，归 M11-6 随修剪同域裁决）**：切割短半段 [1帧, 0.05s) 绕过导出映射阈值（`MIN_SEG_DURATION_SEC` 判空段 → segment:null = 整段保留）→ 用户要碎片段却导出整段源——FR-1735 与 M6-8 空段口径的规格内部冲突，接线后首次用户可达；P2 = C 键修饰键守卫、TIMELINE 更新日志归属修正（§17.5 例外句实落 M11-4）。验证：tsc/eslint 0 · vitest 79 · build · check-docs 五绿（Rust 未动未重跑 cargo）。**手测点**：播放头在片段中段按 C 一拆为二（池+轴各 +1）、贴近边缘 <1 帧按 C 无反应、播放中按 C 切在所见位置、切点非关键帧时两段徽标变 warn（重编码）、Ctrl+C 不触发切割。**已提交**（代码 `97c2dc6` + 本次 `docs:` 回填）。
  - （2026-09-25）**`M11-4`** 选择与波纹删除（**首个行为变更批次**，决策 #13；plans/M11.md §18.5 落地 + 四条实施注记）——波纹删除（块 ✕ / 拖出边界 / Delete）统一 `buildRemoveFromTimeline`：**只出轴、池保留库存、可撤销**；池卡 ✕ = `applyRaw` 真删不经栈（clips + timeline 双清联动；可撤销版 = M11-8 右键「移除并删除池片段」）；`selectedClipId` 改独立 UI state——**修复"成品模式 Delete 恒空"遗留项**，块点击 = 选中 + 进加工（过渡形态，M11-8 后裁）、点空白 = seek + 取消、Delete 全模式可用；`reorderTimeline` 补 `setCheck(null)`（存量错位窗口）。**CR 两轴均 PASS**；采纳：TIMELINE §17.5「唯一例外」扩为两类不可逆联动清理（池真删是第二个绕栈路径）、撤销栈快照复活风险登记 §18.9（M11-7 接线时清洗）。undo 层零改动。**手测点**：块 ✕ / 拖出 / Delete 后**池卡保留**（行为变更核心）、池卡 ✕ 后轴上连带消失、加工视图 Delete 可用、点空白取消选中、接缝连续。**已提交**（代码 `daaa201` + 本次 `docs:` 回填）。
  - （2026-09-25）**`M11-3`** 播放头渲染路径（工程批次，plans/M11.md §18.4 落地 + 九条实施注记）——播放头脱离 React：Workbench 持权威 `playheadRef`，ProductPreview rAF tick **四连直写**（播放头 ref + 时间轴 transform + 进度条 `.value` + 跟随滚动 0.9/0.1 缘触发），播放路径 **0 个 setState**（§17.9②）；React playhead 降频离散镜像（seek/段切换/暂停/结束/复位五同步点，`onPlayheadDiscrete` 事件内直写 transform）；进度条非受控 + `draggingRef` 门控；ClipTimeline 播放头 span 改 transform 定位、注册双 ref；标尺 seek 片段边缘吸附（`snapToEdge` 纯函数，8px/pps，Alt 旁路）；`usePlaybackHotkeys` 加 `getCurrentTime`；切回成品读 `playheadRef` 防丢位置。**CR 两轴均 FAIL 后修至双 PASS**（Spec 2×P0：进度条每帧直写缺失、pause/段切换未进离散汇；教训：降频镜像的离散同步点必须逐路径枚举规格每一项）。**披露**：标尺拖动 scrub 自 M9 未实现（注记⑨归后续批次）。单测 76→79。**手测点**：播放中进度条/读数、暂停对齐、拖动不回弹、Alt 吸附旁路、切模式位置保持、跟随滚动、commit 计数 0/帧。**已提交**（代码 `4a50970` + 本次 `docs:` 回填）。
  - （2026-09-25）**`M11-2`** 缩放滚动（工程批次，plans/M11.md §18.3 落地，六条实施注记见该节）——`overflow-x-auto` + 世界坐标层随内容滚动（播放头/块/刻度**无补偿计算**）；滚轮原生**非 passive** 监听（React 挂 root 的 onWheel 是 passive，preventDefault 无效）：无 Ctrl = 横向滚动、Ctrl+滚轮 = 鼠标锚点缩放（`exp(−deltaY×0.0015)`），scroll 补偿走 `useLayoutEffect([pps])` 同帧无跳动；+/− 键视口中心锚（`KEY_ZOOM_FACTOR=1.2`）、\ 适应窗口；**pps 提为 Workbench 页面级 state**（M11-3 播放头路径的前置），手动缩放前自动跟随适应窗口（M11-1 行为延续）、首次手动操作交还控制权。**CR 两轴：Standards PASS；Spec 先 FAIL 后 PASS**——真 P0 = 接滚动后 `seekAt` 忘加 `scrollLeft`（滚动态点击 seek 系统性偏移；教训：迁移类批次里"逐字节未改动"的代码也会因环境前提变化失效），P1 = 连发滚轮同帧缩放基准取上一档旧 scrollLeft 致锚点漂移（修 = `base = pendingScroll ?? el.scrollLeft`），另 5 条 P2 全修。验证：tsc/eslint 0 · vitest 76 · build · check-docs 五绿。**手测点**：Ctrl+滚轮锚点缩放（指针下时刻不动）、滚轮横滚、+/−/\ 键、放大滚动后点击 seek 落点、素材增删自动适应、拖拽重排回归。**已提交**（代码 `25ea1be` + 本次 `docs:` 回填）。
  - （2026-09-25）**`M11-1`** 精度基座（工程批次，`FR-1730`~`1735` 随批发号进 [TIMELINE.md](./TIMELINE.md) §17.3 行尾）——新增 `ClipTimeline/geometry.ts`：块宽**差值法**（`round(端点×pps) − round(起点×pps)`，相邻块无缝浮点逐位成立，M9-3 重叠根治）、PPS 钳制（硬区间 [2,500] + 动态下限 `6/最短片段`；高帧率源 >83fps 的 1 帧片段可越 500=软区间，注释有裁决并给 `M11-2` 留口）、适应窗口、`tickStep`（≥60px 目标语义；剪切页 `pickTickStep` 是 ≤16 格语义不可混用，plans/M11.md §18.2 公式已更正）；ClipTimeline 删 M9-3 容器比例排布（trackW/MIN_BLOCK_W=28），块/刻度/播放头/seek 全走 geometry，pps 为组件内派生值（`M11-2` 提页面级并接缩放）；刻度标签 m:ss → 总帧数时间码 `time.ts::formatTimecode`（先取总帧再分解，59.99s 帧字段不再溢出）；`utils/perf.ts` 帧时间埋点（5s 窗口 P50/P95/max/drops>1.5×预算：drag=rAF 帧间隔挂 `useDragSort` 新 `onDragActive` 挂点、undo=span 包 `execute`；playing 归 `M11-3`）——M11-9 验收取数就绪。**披露**：块最小宽 28→6px、比例铺满→适应窗口（±1px）、刻度标签变帧时间码（§17.3/§18.2 背书）；动态下限撑出视口暂被 overflow-hidden 裁剪（最短片段占比 <6/视口宽才触发，`M11-2` 接滚动）。两轴 CR 均 PASS，采纳 7 条 P2（硬上限软区间裁决、≥6px 数学下界 5px 披露、seekAt 收口 total 等）。单测 53→**76**（geometry 11/perf 7/time 5）。**手测点**：多片段混排块无缝、空白 seek（右端死区=末尾）、刻度时间码、时间轴四类拖拽与 Merge 列表/素材卡拖拽回归、极短片段动态下限。**已提交**（代码 `13e584f` + 本次 `docs:` 回填）。
  - （2026-09-25）**`T-004`** 三组命令级矩阵固化进 Vitest（工程批次，**无行为变化**，测试载体收口）——新增 `src/utils/crop.test.ts`（`TC-022` `pxToCrop` 静态边界一比一转写 + 4000 组随机不变量扫描；`TC-028` `cropToPx` 三条复现值 + 3 组尺寸 × 2000 组交互选区扫描）与 `src/utils/media.test.ts`（`TC-024` 27 断言一比一：A 九容器 9 / B 其余三维 7 / C 容器串 7 / D wantsProxy 三态 4）；`media.ts` 的 `containerPlayable` 补导出作用例入口。**同域同种子**（20260923）固化；`TC-028` 样本量自 3688320/组缩到 2000/组（边界由静态复现值兜住）、第三尺寸组取最小合法画面 16×16（原记录未留名）——缩减与披露写进用例注释与 [TESTING.md](./TESTING.md) §3.4 注；原脚本 PRNG 未入库无法逐位复现，用例内固定 mulberry32。**顺带**：`TC-040` 计数 19→23（`R2-2` 并入 `productDurationOf` 时漏改）。单测 36→**53**；两轴 CR 均 PASS（采纳 4 条 P2：交互选区措辞改「整数对采样近似——生产路径 CropOverlay 产连续浮点」、cropToPx 扫描补灵敏度守卫等）。**手测**：无 UI 改动无新增；`TC-022`/`TC-024`/`TC-028` 真机半维持待跑。**已提交**（代码 `0f47b58` + 本次 `docs:` 回填）。
  - （2026-09-25）**全仓过度工程审查**（ponytail-audit 口径，**无代码改动**）——两路并行子代理深扫（Rust 后端 / 前端）＋逐条 grep 复核"仅 N 处调用"断言：立案 19 项，全部为重复同构 / 死数据 / 投机灵活性，估算 ≈-345 行源码＋≈-60~80 投机测试、**0 依赖可砍**；最大两块"嫌疑"（撤销读回侧与三个未接线 builder、speed/eta 死链）核实为**已排期半成品**（`M11-4`~`M11-7` / `R3-4`）不立案，`submit_with_cleanup` 因 AGENTS §3 第 8 条红线固化保留。立项 `T-005`（重复收敛 ≈-265 行）/ `T-006`（死代码清理 ≈-80 行＋测试 ≈-70）；全文归档 `docs/archive/code-review-2026-09-25-overengineering.md`（文档提交）。
  - （2026-09-25）**`R2-5`** 死代码清理（工程批次，**无行为变化**；Spec 轴三层证据确认 Probing 删除无历史数据兼容风险——git 全史从未构造过该值、history.json 只落终态、旧数据即便含它也只降级为空列表）——删 `App.css` + main.tsx import、`TaskStatus::Probing`（lib.rs 枚举 + manager.rs 两处 busy-match + 前端 types `"probing"` + services/tauri.ts 关闭守卫比较；**DESIGN §7/§8.1 与 UI.md §9.9 规格同步同批完成**——CR 两轴抓到的 P1）、5 处 `#[allow(dead_code)]` 属性（walkthrough 记 4 处现 5 处；调用者均在，cargo 0 warning）；修 TaskProgress percent 死三目；`fileTimestamp`/`EditorTool` 去 export；package.json 删 `less`、`@tailwindcss/vite`+`tailwindcss` 移 devDependencies（构建期消费、无运行时 import）；types 四字段加"暂未消费"注释（etaSeconds 实为后端 payload 恒发 null）。**走空两项**：CropControls、drawingRef（R1-4 已重构掉）。**⚠ 待用户**：正常终端跑 `pnpm install` 更新 pnpm-lock.yaml 后一并入库。验证：tsc/eslint 0 · vitest 36 · build · cargo 81+5（0 warning）· check-docs 五绿。
  - （2026-09-25）**`R2-4`** ESLint 基线 + `noUncheckedIndexedAccess`（工程批次，**无行为变化**，两轴 CR 逐处核对 44 处判空等价）——`tsconfig.json` 开启 `noUncheckedIndexedAccess`（原标"可选"，选择开启：M11 数组密集操作前的编译期防线）→ 44 处下标访问判空适配（类型层收窄/等价重写，防御分支不可达；`realCutStart` 落点语义零变化）；`eslint.config.js` 升 type-aware：`no-floating-promises`（零命中）+ `no-misused-promises`（揪出剪切页内联 `onClick={async…}` → 抽 `changeDir`）+ `no-unnecessary-type-assertion`（删 3 处冗余断言）；lint 4.1s→9.8s。**披露**：`changeDir` 的 `void` 惯例不补 catch，`pickDirectory` reject 时新旧同为 unhandled rejection（等价）。验证：tsc/eslint 0 · vitest 36 · build 通过。
  - （2026-09-25）**`R2-3`** `useHotkeys` 页面激活门控（工程批次，M11-8/M10-1 前置，**无行为变化**）——`useHotkeys` 加 `enabled = true` 第二参（false **不挂监听**，effect deps `[enabled]`；头注释写明契约：M10-1 keep-alive 落地后各页必须传激活态，否则隐藏页快捷键仍响应 = M10 冲突清单 ③）；`usePlaybackHotkeys` 透传（原 handler 内早退改为不挂监听）；Workbench 成品模式 Delete 的手动 mode gate 换 `enabled` 参（全库唯一"挂载但不激活"调用点，其余 6 处挂载即激活、逐一核对无需接线）。**披露**：异步 mode 转换（removeSource 确认弹窗后）effect-flush 毫秒级窗口内走带键由"响应"变"忽略"（方向保守、不可观测）；**M11-8 接线新键沿用 enabled、勿漏传**。验证：`tsc`/`eslint` 0 · vitest 36 全绿 · `vite build` 通过。门控的挂载/摘除断言待 M10-1 落地时连同 `useTauriEvent` 补 jsdom 测试（记 M10-1 验收项）。
  - （2026-09-25）**`R2-2`** 重复收敛（工程批次，**无行为变化**，四处窄边缘微观差见下）——新增 `src/hooks/useProxyPreview.ts`（Cut / Editor / 工作台两视图**四端共用**；第三参 `fallbackOnVideoError` 保住剪切/编辑器页"`proxyMode !== "off"` 即兜底"的兜底口径，默认 = `useProxy` 与工作台同门；ProductPreview 多路径映射形态不同、不经它）、`src/components/TimeField/index.tsx`（**以 CutEditor 版为准**，删 Workbench 本地版）、`src/hooks/usePlaybackHotkeys.ts`（空格+方向键走带块四端共用；页面专属键 I/O、Delete 留在各自 `useHotkeys`，工作台成品模式 gate 走 `enabled` 参）、`src/utils/array.ts`（`moveAt`）、`src/utils/quality.ts`（`QUALITY_LABELS` 三份并一）；`utils/paths.ts` 加 `basename`（收编四处内联 `split(/[\\/]/)`）与 `resolveUniqueTarget`（三处"同名才追加时间戳"共用，`exists` 注入保持 utils 无 IPC）；`shared.ts` 加 `MIN_SEG_DURATION_SEC`、`commands.ts` 导出 `productDurationOf`（M11-0 留下的两处收敛；Workbench `clipDuration`/`productEntries` 委托之）。**CropOverlay 统一判定已被 R1-4 覆盖**（无剩余重复，无代码）。**披露的微观差**（收敛二选一的窄边缘，两轴 CR 复核接受）：(a) 剪切/编辑器页代理**提交失败**不再卸载文件（旧=probeError+卸载；新=静默保留）；(b) `useProxy` 恒 false 时同路径重载不清已解析代理态；(c) 代理生成时机后移到 info 到达后的 effect，**顺带消除快速换文件时旧代理任务串台到新文件的竞态**；(d) Workbench 时间字段提交未被钳制的输入后文本不再立即规范化。**验证**：`tsc`/`eslint` 0 · vitest **36 全绿**（新增 17 条：array 5 / paths 8 / productDurationOf 4）· `vite build` 通过。**手测点**：三处空格/方向键走带与 I/O 设点；代理开启导入 AVI → 自动代理切换；剪切页时间字段非法值回滚。
  - （2026-09-24）**`R2-1`** Workbench 拆分（工程批次，**无行为变化**）——`src/pages/Workbench/index.tsx` 1636 → **780 行**（净减 856），按职责拆出 10 个文件：`shared.ts`（类型/常量/helper：`SourceFile`·`PreviewMode`·`ClipEdit`·`QUALITY_LABELS`·`NAV_ITEMS`·`fieldBtn`·`freshId`·`newClipOf`·`displayedDims`·`basename`）、`useProxyPreview.ts`、`TimeField` / `SourceCards` / `CutModeView` / `EditModeView` / `Header` / `Footer` / `ClipPool` / `BatchBar`；`index.tsx` 只留编排（state/effects/handlers/derived/组装）。四个既有组件（`SourceCards`/`TimeField`/`CutModeView`/`EditModeView`）签名**逐字保留**，新拆组件 props 全部由页面显式传入（不闭包捕获页面 state）。原列的 "`CropFields` 成文件" **已过时**——Workbench 自 R1-4 起即从 `components/CropOverlay` 导入 `CropFields`，本地无此组件。验证：`tsc --noEmit` 0 · `eslint .` 0 · `vite build` 通过 · `vitest` 19 全绿。**真机冒烟已过**（2026-09-24，CDP 驱动 debug 构建）：首屏空态 → 导入 `123.mp4`（探测 H264）→ 进剪切（入点/出点/添加为片段）→ 建片段（片段池「全段·已在轴」+ 时间轴 1 段 13.4s + 页脚「✓ 全程无损」）→ 加工视图（旋转/放大 tabs）→ 批量条（已选 1 个素材）→ 合成导出，产物 `video/workbench.mp4` 无损（47,124,329 B = 源大小）、`format.duration` 13.403334s、h264+aac 1920×1080、全帧可解码。**已提交**（代码 `fb79736`，文档随本次 `docs:` 提交）。
  - （2026-09-24）**`M11-0`** 状态层/撤销基座（`plans/M11.md` §18.1，**无行为变化**）——新增 `utils/undo/{types,commands,store}.ts`：命令 = **冻结快照对**（`before`/`after`，结构共享）、9 个纯函数 builder（剪出片段 / 追加入轴 / 插入 / 重排 / 移出时间轴 / 移除并删除 / 切割 / 修剪 / 批量合成）、`null` = no-op/取消/非法落点**不入栈**、id 在**构建时**分配并冻进 `after`（重做复现同一 id）；`createUndoStack(limit=100)` 纯核心 + `useUndoStack` React 适配（`applyRaw` 给旁路状态用）。工作台 `clips`+`timeline` 收敛为单一 `EditorDoc`，**六处**结构操作（剪出 / 批量剪出 / 追加入轴 / 拖入插入 / 重排 / 删除片段）改走 `execute`——"删除片段"**保持 M11-0 之前的旧语义（同时出池）**，§17.4 的波纹删除归 M11-4；加工编辑（rot/crop）与素材删除走 `applyRaw`。**测试载体**：Vitest 5.0.1 就位（`pnpm test` = `vitest run`，`vitest.config.ts` 与 `vite.config.ts` 分开），基线 **19 条用例**（六条 Clypra 清单 + 钳制域 + 栈规则）→ `TC-040`。**无行为变化有证据**：临时对照（跑完即删）把"改动前的逐字实现"与命令层放在同一序列上跑 **3000 步随机手势（含 467 次 no-op）→ 文档逐步 deep-equal**，且每个 no-op 都对应"旧实现同样没改文档"。**本次不接线**：Ctrl+Z / 右键撤销项归 M11-7；`buildSplit`/`buildTrim`/`buildRemoveFromTimeline` 已实现并有单测，UI 接线归 M11-5 / M11-6 / M11-4。**已提交**（代码 `7c0b969`、文档 `a02f0d9`）。
  - （2026-09-21）**`R4-8`** 真机首跑抓出的三缺陷（`BUG-007` 吸附值取整导致成品比界面长一个 GOP / `BUG-008` 关键帧丢项 / `BUG-009` 不显示实际落点）——`BUG-007`/`BUG-009` 真机复验通过转 `verified`，`BUG-008` 解析单测已锁、**真机复跑 `TC-030` 待发起**。
  - （2026-09-21）**`R4-1`** 作业体 panic 隔离（`run_job_isolated`）——`BUG-001` 转 `verified`，`TC-019` 通过。连带裁决：release 保留 `panic = "unwind"`（[DECISIONS.md](./DECISIONS.md) `ADR-034`）。
  - （2026-09-21）**`R4-2`** 输出原子替换（`fs::atomic_replace` 6 处）——`BUG-002` 转 `verified`，`TC-020` 通过。
  - （2026-09-21）**`R4-3`** 指针拖拽收尾兜底（新增 `utils/pointerDrag`，四处拖拽共用）——`BUG-003` 转 `fixed`，`TC-021` **手工待跑**。
  - （2026-09-23）**`R4-4`** `pxToCrop` 锚点钳制（锚点上界与收缩步都走 `evenFloor` 向下取偶）——`BUG-004` 转 `fixed`；边界矩阵用**命令级脚本**跑通（14 组期望值 + 4000 组随机扫描 / 12730 断言 0 失败；同矩阵在修复前实现上越界 993/1554），矩阵含期望值与扫描域/种子已记入 [TESTING.md](./TESTING.md) TC-022（**已由 `T-004` 于 2026-09-25 固化为 `src/utils/crop.test.ts`**）；**真机手工（数值微调）待跑**。
  - （2026-09-23）**`R4-9`** `cropToPx` 边缘对齐（`R4-4` 实施时的同族复查发现新缺陷 `BUG-010`）——拖拽框选贴边/单击时原先会超出画面 1~2px（1920 宽下 `x+w=1922`；奇数尺寸 `x=102`），越界只在**作业体内**被拒（"跑到一半才失败"）；改为由**对齐后的边缘**相减得宽高、锚点也收进画面，3 组尺寸 × 3688320 样本越界归零。`BUG-010` 转 `fixed`；**TC-028 真机待跑**；提交期预检仍归 `T-003`。
  - （2026-09-23）**`R4-5`** 缩略图批处理容错（`commands/media.rs` 抽 `thumbnails_into`）——单张抽帧失败由 `return Err` 改 `warn + continue`（与 clip 版同口径，clip 版顺手补 warn；整批性失败仍报错），合并页不再"一个坏文件拖垮整组"。补**命令级回归**（真实 sidecar + lavfi 夹具 + 一个假 mp4），并验证了灵敏度（把 `continue` 换回 `return Err` 该用例立即失败）。`BUG-005` 转 `fixed`；**`TC-023` 的合并页 UI 半待跑**。
  - （2026-09-23）**`R4-6`** 输出容器/扩展名口径（`ADR-033`）——新增 `fs::with_container_ext` / `fs::output_path_for`，逐命令定容器（copy 类跟随源、重编码类固定 mp4），`.part` 与成品同扩展名；`merge`/`pipeline` 的类别取决于探测结果，故容器决策留在**作业体内探测之后**（符合 `DESIGN` §8.1/§8.2 的"按运行时状态检查"）；另修两处：扩展名校正绕过同名检查的静默覆盖路径、`merge`/`pipeline` 的 `add_output` 记未校正路径。回归 `TC-029`（单测 4 条 + e2e 1 条）。已提交。
  - （2026-09-23）**`R4-7`** 代理判定纳入容器维度（`utils/media.ts`）——`needsProxy` 补容器维度（`DESIGN` §10 的规格本就是四维任一不支持）。容器**不能按扩展名判**：`MediaInfo.container` 是 ffprobe `format_name`，即**逗号分隔的候选解复用器列表**且只看内容（实测 mp4/mov/m4v → `mov,mp4,m4a,3gp,3g2,mj2`、mkv/webm → `matroska,webm`、avi→`avi`、flv→`flv`、ts→`mpegts`、wmv→`asf`），故改为**按首位解复用器比对白名单**（ISO-BMFF/QuickTime + Matroska 家族；首位即 ffprobe 选中的那个），**判不出来一律按不可播**（宁可多代理一次也不黑屏）。`DESIGN` §10 的处理范围补齐 FLV/WMV（与 `VIDEO_EXTENSIONS` 对齐）并写明"处理范围按扩展名、预览范围按解复用器名，两套命名空间不可混用"。命令级矩阵 27 条断言 0 失败（九种容器逐一断言，`avi/flv/ts/wmv` 四处由"不代理"翻转为"代理"；另含三维回归、容器串健壮性、`wantsProxy` 三态）；`cargo test` 另锁跨层契约——`container` 必须原样保留 `format_name`（2 条）。`BUG-006` 转 `fixed`；**真机预览半复用 `TC-018`、待发起**。
  - （2026-09-24）**`R3-7`** 输出=输入同一性比较归一化（原属 `R3-6` 第二条，提前实施；**登记为缺陷 `BUG-011`**）——新增 `fs::same_path`（`canonicalize` 两侧；输出不存在时退回"父目录 + 文件名"；都解析不了才退回原样比较），并把**五处守卫收敛为一份**（`fs::reject_if_input_equals` 同时服务提交期与校正后复查，四处命令不再各写一份）。此前全是**逐字符比较** → **大小写不同**（`D:\片\a.mp4` vs `d:/片/A.MP4`）、**末尾点/空格**（`a.mp4.`）、**`..` 绕行**都能绕过，随后 `atomic_replace` 覆盖**源文件**（实测：往 `movie.mp4.` 写入确实改到 `movie.mp4`）。**实测澄清**：`/` 与 `\` 混用、`./` 前缀在 Rust `Path` 里**本来就相等**，不是绕过途径（我原先写在文档里的是错的，已改）。**定性**：守卫意图在改动前的注释里就写明、失败即数据丢失 → **按 BUG 记**（不降级为技术债）。补 4 条单测（`..` / 大小写 / 末尾点空格 / 不同文件不 panic），**灵敏度已验证**（把 `same_path` 临时退回 `a == b` 后 `same_path_normalizes_dotdot_detour` 与 `same_path_ignores_case_on_windows` 立即失败）。单测 77→81。**已提交**（代码 `665bcca`、文档 `ffc2bbe`）。
- **提交状态**：`R4-8`/`R4-1`/`R4-2`/`R4-3`/`R4-4`+`R4-9`/`R4-5`/`R4-6`/`R4-7`/`R3-7`/`M11-0` 已入库（共 21 个提交：`fdff11b` `627cbfc` `a783cca` `3137072` `e5a2f43` `5aeffd4`（ADR-034）· `f278214` · `5fcfd04` · `5fa5d2f`（CR 整改）· `a5f71e1` `06d0130`（R4-4/R4-9）· `412e888` `83a91b2`（R4-5）· `e3053a0` `4b3f7f5`（R4-6）· `c1662cd` `e5b061a`（R4-7）· `665bcca` `ffc2bbe`（R3-7）· `7c0b969` `a02f0d9`（M11-0））；**`R2-1` 已入库**（代码 `fb79736` + 本次 `docs:` 提交）。
- **环境提示**：本机 `pnpm add -D vitest` 落下的包只有空目录（pnpm 建链接失败的既有症状），已用 junction 就地补链（26 处）；用户正常终端跑一次 `pnpm install` 会按官方格式重建 `.bin/vitest`，`pnpm test` 即用。
- **待用户统一手测**：M4-5 NSIS 干净环境冒烟 · M7-1/2/3 · 工作台 2.0 全流程 · 快捷键 · M4-8 · **M9 五项**（连播可 seek / 切换无残留 / 多短片段不压盖且整块可拖 / 加工视图播完出点即停） · **`TC-030` 复跑**（关键帧刻度条数 == ffprobe 原始行数） · **`TC-021`**（拖拽甩出窗口松手） · **`TC-022`/`TC-028` 手工**（裁剪数值框填越界值 / 框选拖到贴边后导出） · **`TC-023` 手工**（合并页混入读不了帧的文件） · **`TC-024` 真机半**（AVI/FLV/TS/WMV 素材导入 → 自动生成代理并正常预览；复用 §3.2 的 `TC-018`） · **R2 批新增**（代理：开启代理导入 AVI → 自动生成并切换，剪切/编辑器/工作台四端；走带：三处空格/方向键与 I/O 设点、工作台成品模式 Delete；`R2-4` 防御性改动：时间轴点击 seek 与插入指示线、更改目录、倍速循环、成品连播跨段、素材卡缩略图——预期无观感变化；`R2-2`：剪切页时间字段非法值回滚）
- **遗留小项**：规则 B 下"有片段裁剪 + 其他片段非恒等旋转"时后者也转码；硬编路径未在真 GPU 验证；旋转覆盖源 flip 元数据；代理关闭时不支持格式仅提示条；日志跨天不切文件；README 截图待补（~~成品模式 Delete 恒空~~ 已随 `M11-4` 独立选中 state 修复，2026-09-25）
- **硬门槛与独立任务**：发布前 asset scope 收窄 + CSP（`CAND-015`）· `T-001` 消除 TIMELINE.md §17.4 ↔ plans/M11.md §18.5 重复 · `T-002` 清理空组件目录（2026-09-25 审查确认删除条件已满足）· `T-003` 审查发现的一致性收敛 · `T-005`/`T-006` 第三轮过度工程审查的重复收敛与死代码清理（`T-004` 矩阵转 Vitest 已于 2026-09-25 完成）

## 未决问题（评审 / 反馈）——待定性

> **硬规则**：会话结束前，每条未决意见**必须转正**（`BUG` / `T` / `ADR` / `FR`）**或显式保留并写原因**；分流规则见 [INDEX.md](./INDEX.md) §6.2（评审产物的归属）。暂存于此的意见须标「待定性」+ 来源/现象/影响面/为何定不了，**不允许无声消失**。

| 项 | 来源 | 现象 / 疑点 | 影响面 | 待定性原因 | 复查点 |
| --- | --- | --- | --- | --- | --- |
| 实机行为未验证 | 评审 §6.2 | `ProductPreview.switchTo` 段边界可能误报 `onPlayingChange(false)` 致连播停住（静态推演判定**不成立**） | 成品连播体验 | 需实机复现；静态审查证不了 | M11-2 / M12-1 实跑连播观察；复现则按"`segIdxRef` 替代 `slot` 比较"处理 |
| 打包与安装未审 | 评审 §8 | `tauri.conf.json` / NSIS / capability 收窄 / CSP 未纳入审查 | **发布前硬门槛** | 属发布面而非功能面；已在 `CAND-015` 立项 | 发布前统一处理（CSP + asset scope 收窄 + 干净环境冒烟） |
| 依赖安全未扫 | 评审 §8 | `pnpm-lock.yaml` / `Cargo.lock` 未做已知漏洞扫描 | 供应链安全 | 需联网工具（本环境代理受限） | 发版前在可联网环境跑一次审计 |
| 性能未 profiling | 评审 §8 | 只有静态开销判断（缩略图冗余请求已转 `T-003`），无实测数据 | 大素材/多片段体验 | 缺 profiling 工具链与夹具 | M11-9（100 片段埋点）时一并取数 |
| e2e 断言强度未评估 | 评审 §8 · §5.3 | `tests/e2e.rs` 断言强度未逐条评估；覆盖缺口：rotate、crop_zoom、merge 非兼容转码分支、proxy 去重 | 回归保障强度 | 需先补齐 R4 回归 TC 才有对照基线 | **R4 已完成（2026-09-23，TC-019–TC-029 就位）、Vitest 载体也已就位（`M11-0`，2026-09-24）——本项已到复查点**；评估时把 `proxy` 去重与 `merge` 非兼容转码分支的缺口一并纳入（`TC-022`/`TC-024`/`TC-028` 的矩阵已由 `T-004` 固化为 Vitest 用例，评估时参照其形态） |
| 位深是否单独展示 | 自查（docs 复查） | DESIGN §3.1 原写面板显示"位深"，实现只有 `pixFmt`（如 `yuv420p10le`，位深含在串里）；Rust `bit_depth` 已提供、前端 TS 未消费。**2026-09-25 过度工程审查已将 `bit_depth` 死字段归 `T-006` 双端删除**（probe 解析→lib.rs→types 全链 0 消费）——届时本行前提变为"字段已删"，若做单列位深则需从 probe 重新解析并三处同补 | 信息面板呈现 | 属产品取舍（单列字段 vs 由像素格式表达），不影响功能 | 提"信息面板增强"需求时一并定；若单列则补 TS 字段 + 面板一行 |
| 缩略图整批失败时的 UI 提示 | `R4-5` 的 CR（Spec 轴 §c2） | 合并页所有文件都抽不出帧时，后端返回空列表、只留日志（前端 `catch` 静默），用户看到的是空白占位图而没有任何文字说明 | 合并页可用性 | 属 UX 增强而非缺陷：与修复前的 `Err` 行为等价（同样被静默 catch），不算回归；要做成"提示条"需先定文案与位置 | 提"合并页体验"批次时定；若做则前端加提示条 + TC-011 补断言 |
| 工作台中间件固定 mp4 与"copy 跟随源容器"的缺口 | `R4-6` 的 CR（Standards 轴） | 工作台的**中间文件**（`.pipe_*`）仍固定 `.mp4`，而 ADR-033 ① 让全 copy 拼接的**成品**跟随源容器。源为 mkv 且视频编码是 mp4 不容纳的（如 VP9）时，`-c copy` 写中间 mp4 会失败——"跟随源容器"对该类源实际不可达 | 非 mp4 系编码素材的工作台导出 | 非本轮引入（中间件一直是 mp4，`FFMPEG.md` §6.3⑨⑩ 也是这么定的）；改法是把 copy 片段的中间件也按源容器命名，但会让 concat 读入混合容器，需要真实素材（VP9/Opus 的 mkv）验证后再动 | 有该类素材做真机验证时一并定；届时同步 `FFMPEG.md` §6.3⑨⑩ |
| 界面显示的输出名 vs 实际落盘名 | `R4-6` 的 CR（Spec 轴） | 容器校正发生在后端，所以界面上写的名字可能和落盘的不一样：合并页/工作台默认名固定 `.mp4`（源是 mkv 时落盘变 `.mkv`）、任务面板 label 用的是**提交时请求名**、编辑器页已按同口径生成名字（本轮已修） | 界面一致性（不影响产物正确性） | 要让合并页/工作台也预判容器，得在前端复制"copy/transcode 分类"逻辑（需 `check_pipeline` 结果 + `compatible`/`forceTranscode`），当前无前端测试基建 | 提"导出体验"批次时定；若做则前端按同一口径生成名字 + 任务 label 用实际路径 |
| H.265 装了系统扩展也被强制代理 | `R4-7` 的 CR（Standards 轴） | `DESIGN` §10 表写 H.265 是"**依赖系统 HEVC 扩展**，探测失败 → 代理"，而 `needsProxy` 是**静态白名单**（h265 不在 `NATIVE_VIDEO`，一律判需要代理）；仓库里没有 `canPlayType` 之类运行时探测，只有播放失败后的 `onError` 兜底。**净效果 = 宁可多生成一次代理**：装没装扩展都会转码（只影响耗时/临时磁盘，预览与导出结果都正确），反向（判"可播"却播不了）由 `onError` 兜住 | 装了 HEVC 扩展的机器上，H.265 素材预览要多等一次代理生成 | 非本轮引入（静态判定从 M1-8 起就这样），且方向保守、不破坏正确性；要做准需在 WebView2 里跑 `canPlayType` 探测（属真机/GUI 面） | M12 预览强化时定；若做则把 `canPlayType` 结果并入 `needsProxy`（并同步 `DESIGN` §10 的表述） |

## 关键事实（M11 阶段；长期红线与坑速查见 AGENTS.md §3/§8）

- **Rust 类型契约集中在 `src-tauri/src/lib.rs`**（serde `rename_all = "camelCase"`；枚举 snake_case，数字前不加下划线）：`PipelineItem { input, segment?, rotateDeg, hflip, vflip, crop?, outWidth?, outHeight? }`；除 Merge 外四类任务均带 `encoder: Option<String>`。加字段需同提交更新 `lib.rs` + `src/types` + DESIGN §7。
- **sidecar 运行时命名是裸 `ffmpeg.exe` / `ffprobe.exe`**；`resolve_sidecar` 已对齐插件行为，测试二进制需上溯 deps 目录。
- **任务系统**：并发 2、kill 取消、`.part`→rename；事件 `task-status` / `task-progress`（payload camelCase）；`submit_pipeline` 有 debug 日志打印收到的载荷（e2e 排查时看日志文件或 tauri dev 控制台）。
