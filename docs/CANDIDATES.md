# video-cut 功能候选池（CANDIDATES）

> **职责**：未排期的功能/工程候选清单及其说明、量级、"不做"清单。
> **唯一真源**：**候选池的唯一真源**（含每条说明与量级）；[PLAN.md](./PLAN.md) 只引用编号不复制说明。
> **读时机**：提新需求、排下一批、判断"这个想法记过没有"时。
> **写规则**：只追加候选行；**候选不是承诺**——晋升为里程碑前必须先在 [DESIGN.md](./DESIGN.md) 补完整设计（数据模型/命令/UI）并过决策记录（[DECISIONS.md](./DECISIONS.md)）；旧编号 `B1–B19` 与本表 `CAND-001–019` 一一对应（双号并存，旧引用不重写），**新候选从 `CAND-020` 起**。
> **关联**：[INDEX.md](./INDEX.md)（ID 规范） · [DECISIONS.md](./DECISIONS.md)（决策） · [PLAN.md](./PLAN.md)（排期）
> **最后更新**：2026-09-19（自 DECISIONS.md §16 迁出，正文未改动）

> 本节原为 `DECISIONS.md` §16（2026-09-14 整理）；编号与内容逐字保留，仅迁址。

## 16. 功能候选池（Backlog，2026-09-14 整理，未定稿）

> **状态说明**：本节是候选清单，不是承诺。条目晋升为里程碑前，需在 [DESIGN.md](./DESIGN.md) 补充完整设计（数据模型/命令/UI）并过决策记录；**本节是候选池的唯一真源**，[PLAN.md](./PLAN.md) 只引用编号不复制说明。排序 = 建议优先级；量级为单人估时参考。
>
> **已晋升**：B1、B2、B14 → M8（2026-09-14，已实现）；B17 → M10-2（2026-09-15，待实施，设计见 UI.md §9.1 / 决策 #25）；B10、B11 → M11（2026-09-16 并入，决策 #29，设计见 TIMELINE.md §17）；下表保留原条目并标注去向。

### 状态取值

| 状态 | 含义 | 何时改 |
| --- | --- | --- |
| `idea` | 记下来但未评估 | 新增候选时 |
| `evaluating` | 已讨论过、待排期（有价值但优先级未定） | 评估后 |
| `accepted` | 已采纳——含已晋升为里程碑 / 已实施（去向写在「候选」列的 `→ M8 已实现` 等字样里） | 晋升或实施时 |
| `parked` | 主动搁置（价值有但工程量/时机不合适） | 讨论后 |
| `rejected` | 明确不做（含文件末尾「不做」清单） | 裁决后 |

> 状态列只给机读值；**去向与理由仍在「候选/说明」列**，不重复。

### A. 高性价比（建议下一批）

| # | 候选 | 说明 | 量级 | 状态 |
| --- | --- | --- | --- | --- |
| CAND-001（原 B1） | ~~probe / 关键帧索引缓存~~ → M8 已实现 | 按 `路径+size+mtime` 缓存 MediaInfo 与关键帧列表；重复打开同一文件秒出（现每次进剪切页重扫数秒） | 小 | accepted |
| CAND-002（原 B2） | ~~磁盘空间预检推广到全部任务~~ → M8 已实现 | cut 已有 fs4 预检（DESIGN.md §8.2），抽公共函数接入 merge/rotate/crop/pipeline——空间不足不再编码中途失败留半成品 | 小 | accepted |
| CAND-003（原 B3） | 缩略图 / 关键帧并行生成 | `generate_thumbnails_sync` 串行循环改线程池并行，素材多时首次加载提速 | 小 | idea |
| CAND-004（原 B4） | 帧截图导出 | 预览画面「保存当前帧为 PNG」（canvas drawImage），与局部放大功能天然配套 | 极小 | idea |
| CAND-005（原 B5） | 窗口状态记忆 | tauri-plugin-window-state 一行接入，记住大小/位置/最大化 | 极小 | idea |
| CAND-006（原 B6） | 音频提取 / 音轨替换 | `-map` remux 级无损：提取音轨为 m4a/mp3、给视频换/删音轨；复用现有任务系统。**文件级工具，与时间线音频编辑无关**（后者不做，决策 #27） | 中小 | evaluating |

### B. 值得做（按需求热度）

| # | 候选 | 说明 | 量级 | 状态 |
| --- | --- | --- | --- | --- |
| CAND-007（原 B7） | Smart cut（自"远期候选"升格） | 精确剪切仅重编码切点附近 GOP，其余 stream copy——画质/速度/精确三点全占；复用 plan_items 分段思想、timescale 处理、临时令牌；难点在 GOP 解析与音画对齐 | 大 | parked |
| CAND-008（原 B8） | 导出预设 | 一键参数档（"高兼容 1080p H.264"/"B 站投稿"等），底层 = 编码参数映射表扩展 | 中 | idea |
| CAND-009（原 B9） | GIF / WebM 短片段导出 | 剪切区间选择 UI 全部现成，扩展输出格式；表情包高频场景 | 中小 | idea |
| CAND-010（原 B10） | ~~时间轴块修剪手柄~~ → M11（2026-09-16 并入，决策 #29） | 时间线边缘修剪 = M11-6（TIMELINE.md §17.4）：热区光标、双向波纹修剪、关键帧吸附联动 | 中 | accepted |
| CAND-011（原 B11） | ~~工作台撤销栈~~ → M11（2026-09-16 并入，决策 #29） | 命令模式快照对（TIMELINE.md §17.5），覆盖切割/删除/修剪/重排/移入移出 | 中 | accepted |
| CAND-012（原 B12） | 任务完成通知 | tauri-plugin-notification 系统通知 + 可选自动打开输出目录；批量场景价值上升 | 小 | evaluating |
| CAND-013（原 B13） | 字幕提取 .srt | 精确剪切丢弃字幕流的补偿：`-map 0:s -c:s srt` 导出 | 小 | idea |
| CAND-018（原 B18） | 片段复制 / 粘贴（2026-09-17 自右键菜单稿筛出） | 右键或快捷键复制片段（区间 + 变换整体），在播放头处粘贴插入；命令栈 builder 易加，属七操作之外的便利项（注意与 TIMELINE.md §17.10"复制/粘贴**变换**"不做不冲突——那是仅变换字段） | 小 | evaluating |
| CAND-019（原 B19） | 片段属性面板（2026-09-17 自右键菜单稿筛出） | 右键「属性」弹层：源信息 / 区间与帧数 / copy·transcode 判定原因全量展示（徽标与块 tooltip 的完整版），服务"好用 = 导出快"的透明度 | 小 | evaluating |
| CAND-017（原 B17） | ~~深浅主题切换~~ → M10-2 待实施 | 亮色变量集整体替换 + 对比度校对（原 DESIGN §14 阶段 4 既有项，现 PLAN M10-2）；与主题色基建（UI.md §9.1）同源；三态深色/浅色/跟随系统（决策 #25） | 中 | accepted |

### C. 工程质量

| # | 候选 | 说明 | 量级 | 状态 |
| --- | --- | --- | --- | --- |
| CAND-014（原 B14） | ~~核心链路 e2e 自动化~~ → M8 已实现 | 用夹具真实跑「剪切→合并→pipeline」断言输出时长/可解码；防重构回归的保险（落地形式见 FFMPEG.md §6.6 / 决策 #23） | 中 | accepted |
| CAND-015（原 B15） | 评审遗留 P2 收尾 | 调度线程 panic 加固、16bit 高位深判定、**asset scope 收窄 + CSP（公开发布前必做）**、check_pipeline facts 缓存（已并入 M12-2）、expand_video_inputs 条数上限（明细见 [PLAN.md](./PLAN.md) 评审修复批次 R3） | 中 | evaluating |
| CAND-016（原 B16） | README 截图 | 界面稳定后补功能截图 | 小 | idea |

### 不做（记录避免反复讨论，状态 = `rejected`）

- 界面多语言 i18n（用户群明确）　`rejected`
- 多轨时间线编辑器、特效/转场/字幕编辑（DESIGN.md §1.3 非目标）　`rejected`
- 时间线音频轨编辑（波形/音量线/淡入淡出/分离/替换）、空隙模型、禁用/黑场片段、调色、命令面板、覆盖/插入双编辑、J 倒放（TIMELINE.md §17.10，决策 #26/#27）　`rejected`
